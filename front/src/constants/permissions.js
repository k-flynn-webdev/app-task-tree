
const PERMS_BASE = {
  owner: {
    read: true,
    write: true,
    update: true,
    progress: true,
    delete: true,
    share: true
  }
}

const ANON = 'anon' // can use all except share
const USER = 'user'
const ADMIN = 'admin'

export default {

}
